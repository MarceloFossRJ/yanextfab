# Copy to .env and fill in. Used by docker-compose.yml for local development.

# Random long string used to sign JWTs. Generate with: openssl rand -hex 32
JWT_SECRET=dev-secret-change-me-before-deploying-anywhere-real

# Required for the AI agent example to call Claude. Leave empty to disable it locally.
ANTHROPIC_API_KEY=
